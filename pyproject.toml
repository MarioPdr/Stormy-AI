[project]
name = "stormy-ai"
version = "1.0.0"
description = "Stormy AI Discord Bot"
requires-python = ">=3.12"
dependencies = [
    "openai",
    "PyNaCl"
]

[tool.vercel]
entrypoint = "api.discord:handler"
